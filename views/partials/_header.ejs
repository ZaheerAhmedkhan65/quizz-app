<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="shortcut icon" href="/img/favicon/favicon.png" type="image/x-icon">
    <link rel="stylesheet" href="/css/bootstrap-icons.min.css">
    <title><%= title %></title>
</head>
<body>

    <!-- Desktop Navbar (Visible on medium and larger screens) -->
    <nav class="navbar fixed-top navbar-expand-md bg-primary d-none d-md-block">
        <div class="container-fluid">
            <a class="navbar-brand text-white" href="/api/dashboard">QuizApp</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/api/dashboard/">Dashboard</a>
                    </li>
                    <% if(user) { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown">
                                <%= user.username %>
                            </a>
                            <ul class="dropdown-menu">
                                <li>
                                    <a class="dropdown-item text-danger" href="/auth/logout" onclick="return confirm('Are you sure you want to logout?')">Logout</a>
                                </li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link text-white" href="/auth/login">Log In</a></li>
                        <li class="nav-item"><a class="nav-link text-white" href="/auth/create-account">Create Account</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Mobile Navbar (Visible only on small screens) -->
    <nav class="navbar fixed-top bg-primary d-md-none">
        <div class="container-fluid">
            <a class="navbar-brand text-white" href="/api/dashboard">QuizApp</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="offcanvas offcanvas-end" id="offcanvasNavbar">
                <div class="offcanvas-header">
                    <h5 class="offcanvas-title">
                        <a class="navbar-brand text-dark" href="/api/dashboard">QuizApp</a>
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="offcanvas"></button>
                </div>
                <div class="offcanvas-body">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="/api/dashboard/">Dashboard</a>
                        </li>
                        <% if(user) { %>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                    <%= user.username %>
                                </a>
                                <ul class="dropdown-menu">
                                    <li>
                                        <a class="dropdown-item text-danger" href="/auth/logout" onclick="return confirm('Are you sure you want to logout?')">Logout</a>
                                    </li>
                                </ul>
                            </li>
                        <% } else { %>
                            <li class="nav-item"><a class="nav-link" href="/auth/signin">Sign In</a></li>
                            <li class="nav-item"><a class="nav-link" href="/auth/signup">Sign Up</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <div class="container-fluid mt-5">
        <div class="row pt-3">